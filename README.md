# 📱 MarZlog - Expo Universal App

AI 기반 사진 관리 앱 (iOS + Android + Web)

## 🚀 빠른 시작

### 1. 의존성 설치

```bash
npm install
```

### 2. 환경 변수 설정

```bash
cp .env.example .env
# .env 파일에서 API URL 등 설정
```

### 3. 개발 서버 실행

```bash
# 모든 플랫폼
npx expo start

# 웹만
npm run web

# iOS만 (Mac 필요)
npm run ios

# Android만
npm run android
```

---

## 📦 프로젝트 구조

```
marzlog-app/
├── app/                    # Expo Router 페이지
│   ├── _layout.tsx         # 루트 레이아웃
│   ├── index.tsx           # 진입점 (리다이렉트)
│   ├── (auth)/             # 인증 화면
│   │   ├── onboarding.tsx
│   │   └── login.tsx
│   ├── (main)/             # 메인 탭
│   │   ├── timeline.tsx
│   │   ├── search.tsx
│   │   ├── albums.tsx
│   │   └── profile.tsx
│   ├── photo/[id].tsx      # 사진 상세 (모달)
│   └── album/[id].tsx      # 앨범 상세
│
├── src/                    # 소스 코드
│   ├── api/                # API 클라이언트
│   ├── components/         # 공유 컴포넌트
│   ├── store/              # Zustand 상태
│   ├── theme/              # 디자인 시스템
│   ├── hooks/              # Custom Hooks
│   ├── utils/              # 유틸리티
│   ├── types/              # TypeScript 타입
│   └── constants/          # 상수
│
├── assets/                 # 정적 리소스
├── app.json                # Expo 설정
├── eas.json                # EAS Build 설정
├── amplify.yml             # AWS Amplify 설정
└── package.json
```

---

## 🌐 AWS 배포 (3가지 방법)

### 방법 1: AWS Amplify (가장 간단) ⭐

1. **GitHub 연결**
   - AWS Console → Amplify → New App
   - Connect GitHub repository
   - Branch: `main`

2. **자동 배포**
   - `amplify.yml` 자동 감지
   - Push → 자동 빌드 → 배포
   - 커스텀 도메인 설정 가능

```bash
# 예상 비용: ~$0/월 (소규모)
# 장점: 가장 간단, HTTPS 자동, CDN 포함
```

### 방법 2: S3 + CloudFront

1. **S3 버킷 생성**
```bash
aws s3 mb s3://marzlog-web --region ap-northeast-2
```

2. **정적 웹사이트 호스팅 활성화**
```bash
aws s3 website s3://marzlog-web \
  --index-document index.html \
  --error-document index.html
```

3. **CloudFront 배포 생성**
```bash
# AWS Console에서 CloudFront 배포 생성
# Origin: S3 버킷
# SPA를 위해 403/404 → index.html 리다이렉트 설정
```

4. **GitHub Actions로 자동 배포**
```yaml
# .github/workflows/deploy-web.yml 사용
# Secrets 설정 필요:
# - AWS_ACCESS_KEY_ID
# - AWS_SECRET_ACCESS_KEY
# - AWS_REGION
# - S3_BUCKET_NAME
# - CLOUDFRONT_DISTRIBUTION_ID
```

### 방법 3: Vercel (대안)

```bash
# Vercel CLI 설치
npm i -g vercel

# 배포
vercel --prod
```

---

## 📱 앱 빌드 (iOS/Android)

### EAS Build 설정

```bash
# EAS CLI 설치
npm install -g eas-cli

# 로그인
eas login

# 프로젝트 연결
eas init

# 빌드
eas build --platform ios     # iOS
eas build --platform android # Android
eas build --platform all     # 둘 다
```

### 스토어 제출

```bash
# App Store
eas submit --platform ios

# Play Store
eas submit --platform android
```

---

## 🔐 환경 변수

| 변수 | 설명 | 필수 |
|------|------|------|
| `EXPO_PUBLIC_API_URL` | 백엔드 API URL | ✅ |
| `EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID` | Google OAuth (Web) | ✅ |
| `EXPO_PUBLIC_GOOGLE_IOS_CLIENT_ID` | Google OAuth (iOS) | iOS |
| `EXPO_PUBLIC_GOOGLE_ANDROID_CLIENT_ID` | Google OAuth (Android) | Android |

---

## 🛠️ 개발 명령어

| 명령어 | 설명 |
|--------|------|
| `npm start` | Expo 개발 서버 |
| `npm run web` | 웹 개발 서버 |
| `npm run ios` | iOS 시뮬레이터 |
| `npm run android` | Android 에뮬레이터 |
| `npm run build:web` | 웹 프로덕션 빌드 |
| `npm run lint` | ESLint 검사 |
| `npm run type-check` | TypeScript 검사 |

---

## 📊 아키텍처

```
┌─────────────────────────────────────────────────────────┐
│                    MarZlog System                        │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  ┌──────────────────────────────────────────────────┐   │
│  │              Frontend (Expo)                      │   │
│  │  ┌─────────┐ ┌─────────┐ ┌─────────┐            │   │
│  │  │   iOS   │ │ Android │ │   Web   │            │   │
│  │  │  App    │ │   App   │ │  (SPA)  │            │   │
│  │  └────┬────┘ └────┬────┘ └────┬────┘            │   │
│  │       │           │           │                  │   │
│  │       └───────────┼───────────┘                  │   │
│  │                   │                              │   │
│  │           ┌───────▼───────┐                     │   │
│  │           │  Expo Router  │                     │   │
│  │           │  + Zustand    │                     │   │
│  │           └───────┬───────┘                     │   │
│  └───────────────────┼──────────────────────────────┘   │
│                      │                                   │
│                      │ HTTPS                             │
│                      ▼                                   │
│  ┌──────────────────────────────────────────────────┐   │
│  │              Backend (FastAPI)                    │   │
│  │                                                   │   │
│  │  ┌─────────┐ ┌─────────┐ ┌─────────┐            │   │
│  │  │  Auth   │ │ Timeline│ │ Search  │            │   │
│  │  │  API    │ │   API   │ │   API   │            │   │
│  │  └─────────┘ └─────────┘ └─────────┘            │   │
│  │                                                   │   │
│  │  ┌─────────┐ ┌─────────┐ ┌─────────┐            │   │
│  │  │PostgreSQL│ │  Redis  │ │  MinIO  │            │   │
│  │  │+pgvector│ │ (Queue) │ │  (S3)   │            │   │
│  │  └─────────┘ └─────────┘ └─────────┘            │   │
│  └──────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘
```

---

## 📝 라이선스

MIT License

---

**작성일**: 2025-12-15
**버전**: 1.0.0
